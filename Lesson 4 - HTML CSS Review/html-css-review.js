// Know how to use these tools:

// HTML (HyperText Markup Language) - Creates the structure of a webpage
// CSS (Cascading Style Sheets) - Styles the webpage (colors, fonts, layout, etc.)
// JavaScript - Adds interactivity to the webpage (buttons, forms, animations, etc.)

alert('Hello, world!'); // This is a JavaScript alert that will pop up when the page loads.